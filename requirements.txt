# ZeroDTE backend dependencies
# Install: python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Core
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=13
httpx>=0.27
python-dotenv>=1.0
numpy>=1.26
pydantic>=2.7

# IBKR live data + options chain
ib_insync>=0.9.86

# Macro news
# (Finnhub has a 60 calls/min free tier; using httpx directly, no SDK)

# TradingView enrichment (external TA for confluence scoring)
tradingview-ta>=3.3
yfinance>=0.2

# Local DB for paper-trade log
# (sqlite3 is in stdlib, no install needed)
